


Version visee dev local. tourne dans un docker et non une VM



Fait tourner K3S dans des dockers. 




Machine réelle
└── Docker daemon
    ├── Container "k3s-server"   ← simule un node "machine"
    ├── Container "k3s-agent-0"  ← simule un autre node
    ├── Container "k3s-agent-1"  ← simule un troisième node
    └── Container "loadbalancer" ← entrée réseau



Demarrage bien plus rapide. pour les tests rapide.